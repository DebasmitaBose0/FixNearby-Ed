// Legacy booking expiry checker placeholder
// Deprecated in favor of the automated booking expiry background service.
export const checkBookingExpiryLegacy = (bookingId) => {
  console.warn("Using deprecated legacy booking expiry checker.");
  return false;
};
